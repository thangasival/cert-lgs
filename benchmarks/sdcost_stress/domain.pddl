; SDCostStress domain — generated by generate_sdcost_stress.py
; Purpose: stress-test state-dependent cost handling in certifiers C3 and C4.
(define (domain sdcost-stress)
  (:requirements :conditional-effects :action-costs)
  (:predicates
    (cost-state-0)
    (cost-state-1)
    (cost-state-2)
    (cost-state-3)
    (cost-state-4)
    (op-0-done)
    (op-1-done)
    (op-2-done)
    (op-3-done)
    (op-4-done)
    (op-5-done)
    (op-6-done)
    (op-7-done))
  (:functions (total-cost))
  (:action do-op-0
    :parameters ()
    :precondition (not (op-0-done))
    :effect (and (op-0-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-1
    :parameters ()
    :precondition (not (op-1-done))
    :effect (and (op-1-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-2
    :parameters ()
    :precondition (not (op-2-done))
    :effect (and (op-2-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-3
    :parameters ()
    :precondition (not (op-3-done))
    :effect (and (op-3-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-4
    :parameters ()
    :precondition (not (op-4-done))
    :effect (and (op-4-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-5
    :parameters ()
    :precondition (not (op-5-done))
    :effect (and (op-5-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-6
    :parameters ()
    :precondition (not (op-6-done))
    :effect (and (op-6-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
  (:action do-op-7
    :parameters ()
    :precondition (not (op-7-done))
    :effect (and (op-7-done)
                 (increase (total-cost) 1)
             (when (cost-state-0) (increase (total-cost) 1))
             (when (cost-state-1) (increase (total-cost) 2))
             (when (cost-state-2) (increase (total-cost) 3))
             (when (cost-state-3) (increase (total-cost) 4))
             (when (cost-state-4) (increase (total-cost) 5))))
)
