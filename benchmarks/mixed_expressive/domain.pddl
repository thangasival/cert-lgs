; MixedExpressive domain — generated by generate_mixed.py
; All three expressive features: conditional effects, axioms, state-dependent costs.
(define (domain mixed-expressive)
  (:requirements :conditional-effects :derived-predicates :action-costs)
  (:predicates
    (b0)
    (b1)
    (b2)
    (b3)
    (b4)
    (b5)
    (b6)
    (b7)
    (d0)
    (d1)
    (d2)
    (d3)
    (cf0)
    (cf1)
    (cf2)
    (done0)
    (done1)
    (done2)
    (done3)
    (done4)
    (done5)
    (done6))
  (:functions (total-cost))
  (:derived (d0)
    (and (b0) (b1)))
  (:derived (d1)
    (and (d0) (b2)))
  (:derived (d2)
    (and (d1) (b3)))
  (:derived (d3)
    (and (d2) (b4)))
  (:action step-0
    :parameters ()
    :precondition (and (b0) (not (done0)))
    :effect (and (done0)
                 (b1)
                 (not (b0))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b2) (b3))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf0) (increase (total-cost) 2))))
  (:action step-1
    :parameters ()
    :precondition (and (b1) (not (done1)))
    :effect (and (done1)
                 (b2)
                 (not (b1))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b3) (b4))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf1) (increase (total-cost) 2))))
  (:action step-2
    :parameters ()
    :precondition (and (b2) (not (done2)))
    :effect (and (done2)
                 (b3)
                 (not (b2))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b4) (b5))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf2) (increase (total-cost) 2))))
  (:action step-3
    :parameters ()
    :precondition (and (b3) (not (done3)))
    :effect (and (done3)
                 (b4)
                 (not (b3))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b5) (b6))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf0) (increase (total-cost) 2))))
  (:action step-4
    :parameters ()
    :precondition (and (b4) (not (done4)))
    :effect (and (done4)
                 (b5)
                 (not (b4))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b6) (b7))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf1) (increase (total-cost) 2))))
  (:action step-5
    :parameters ()
    :precondition (and (b5) (not (done5)))
    :effect (and (done5)
                 (b6)
                 (not (b5))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b7) (b0))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf2) (increase (total-cost) 2))))
  (:action step-6
    :parameters ()
    :precondition (and (b6) (not (done6)))
    :effect (and (done6)
                 (b7)
                 (not (b6))
                 ; conditional effect — sets next-next fluent if current CE holds
                 (when (b0) (b1))
                 ; base cost
                 (increase (total-cost) 1)
                 ; state-dependent cost: +2 if cost fluent active
                 (when (cf0) (increase (total-cost) 2))))
)
