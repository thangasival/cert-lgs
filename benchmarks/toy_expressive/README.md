# Toy expressive benchmark

This placeholder benchmark exists to keep the starter pipeline runnable.

Replace it with valid expressive PDDL domains containing:

- conditional effects,
- derived predicates / axioms,
- state-dependent action costs.

For JAIR-level work, this folder is not enough. You need a serious benchmark suite.
